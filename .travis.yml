language: java
sudo: false
install: true

cache:
 directories:
   - $HOME/.m2

jdk:
  - oraclejdk9

script:
  - mvn --errors clean:clean compiler:compile resources:resources native2ascii:resources sonar-packaging:sonar-plugin

addons:
  sonarqube: true
  apt:
    packages:
      - oracle-java9-installer

notifications:
  email:
    recipients:
      - 37671934+LightSlateGray@users.noreply.github.com
    on_success: always
    on_failure: always